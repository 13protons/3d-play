# UI Overlay

HTML/CSS overlay components rendered outside the R3F canvas. HUD elements, menus, and controls that don't need 3D rendering.

## Files

- `HUD.tsx` — Flight instruments: speed, altitude, warp rate, fuel, temperatures
- `MainMenu.tsx` — Mode selection, scenario loading
