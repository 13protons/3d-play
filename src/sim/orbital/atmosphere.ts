/**
 * Atmospheric density and temperature models.
 *
 * Each body can reference a named atmosphere model. The orbital worker
 * samples these models to generate the atmosphere section of EnvironmentPatch.
 */
